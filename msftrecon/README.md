# msftrecon
https://github.com/Arcanum-Sec/msftrecon

## Usage

**Build**
```
docker build -f Containerfile -t msftrecon .
```

**Run**
```
docker run --rm -it msftrecon
docker run --rm -it msftrecon -d example.com
```
